#include <iostream> #include <string> int main() {
int порядковыйНомер;
std::cout << "Введите порядковый номер щелочного металла (Li, Na, K, Rb, Cs): "; std::cin >> порядковыйНомер;
std::string цветПламени;
switch (порядковыйНомер) {
case 3:
цветПламени = "красный"; break;
case 11:
цветПламени = "фиолетовый"; break;
case 19: case 37: case 55:
цветПламени = "фиолетовый (с розовым оттенком)";
break; case 87:
цветПламени = "фиолетовый (синий оттенок)";
break; default:
цветПламени = "неизвестно";
break; }
if (цветПламени != "неизвестно") {
std::cout << "Цвет пламени для щелочного металла: " << цветПламени << std::endl;
} else {
std::cout << "Данный порядковый номер не соответствует щелочному металлу." << std::endl;
}
return 0; }
